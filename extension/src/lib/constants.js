/**
 * Default API base for production.
 * Uses the Render default domain wildcard allowed in manifest host_permissions.
 */
export const DEFAULT_API_BASE = "https://express-deploy-test-kbkd.onrender.com";
