/**
 * Runs in the page context for matched origins (see manifest).
 * Keep feature-specific DOM logic here; call the background or api-client via messages if needed.
 */
console.log("[APA Asistente] account-hints content script loaded");
